[package]
name = "prove_score_improved"
type = "bin"
authors = ["PrivateScore Team"]
compiler_version = ">=0.30.0"
description = "ZK proof that credit score has increased using two-commitment comparison"

[dependencies]